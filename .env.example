# Engage backend base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:4173

# Default account SID
VITE_ACCOUNT_SID=

# Optional: API key + token for Basic auth (if not using cookies)
VITE_API_KEY=
VITE_API_TOKEN=
